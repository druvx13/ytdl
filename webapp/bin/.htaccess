# Block all direct HTTP access to binaries in this directory.
# yt-dlp (or youtube-dl) is executed by PHP server-side only.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
